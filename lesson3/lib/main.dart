import 'package:flutter/material.dart';
import 'view/homepage.dart';

void main() => runApp(Lesson3App());

class Lesson3App extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      home: HomePage(),
    );
  }

}