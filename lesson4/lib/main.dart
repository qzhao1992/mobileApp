import 'package:flutter/material.dart';
import './view/homepage.dart';

void main() => runApp(Lesson4App());

class Lesson4App extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
  

}